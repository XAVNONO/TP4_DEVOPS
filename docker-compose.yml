version: '3'

services:
  scout-cli:
    image: docker/scout-cli
    ports: 
      - "11434:11434"
    command : >
      sh -c'echo dckr_pat_4J2oyGlyWzsg8HafBxz4YnTOqhQ | docker login -u xavnono --password-stdin &&
        docker-scout cves xavnono/python_app --exit-code --only-severity critical,high'
